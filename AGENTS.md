# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Backend Functions Folder

Always use the `Coram-Deo-Backend` folder for mobile function updates, changes, and deployments so that it will also always align to web.
